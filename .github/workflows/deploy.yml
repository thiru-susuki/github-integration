name: Deploy to Staging

on:
  pull_request:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Jira Deployment Notification
        run: echo "Deployment to staging complete for MOB issues"
